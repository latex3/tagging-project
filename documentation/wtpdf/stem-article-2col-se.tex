\DocumentMetadata{
  lang          = en,
  pdfversion    = 2.0,
  pdfstandard   = UA-2,
  pdfstandard   = A-4f, % even with SE this requires 4f because of embedded CSS
  tagging-setup = {math/setup=mathml-SE, extra-modules={verbatim-mo}}
}

\input{stem-article-2col}
